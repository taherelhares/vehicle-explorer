namespace VehicleExplorer.Application.Models;

/// <summary>A class of vehicle a manufacturer produces, in this application's vocabulary.</summary>
public sealed record VehicleTypeDto(int Id, string Name);
